# Copy to `.dev.vars` for local `wrangler dev` (never commit the real values).
# In production these are set with `wrangler secret put <NAME>` instead.

OPENAI_API_KEY=sk-your-openai-key
APP_ACCESS_TOKEN=choose-a-long-random-string-and-put-the-same-value-in-the-app-env
